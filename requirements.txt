# =============================================================================
# tw-stock-collector 依賴清單
# Python: >=3.11(用到 tomllib stdlib、type hint X | Y syntax)
# 安裝:pip install -r requirements.txt
# =============================================================================

# --- 核心(原本就用的)---
aiohttp>=3.9                # FinMind HTTP client(api_client.py)

# --- 新增 v2.0(Postgres 遷移)---
psycopg[binary]>=3.2,<4     # Postgres driver(同步;[binary] 自動裝編譯好的二進位)
psycopg-pool>=3.2           # Connection pool(目前 db.py 沒用,Aggregation Layer 會用)
alembic>=1.13               # Schema migration 工具
python-dotenv>=1.0          # 載入 .env 檔(讓 DATABASE_URL 等環境變數從檔案進來)

# --- 開發 / 測試 ---
pytest>=8.0                 # 單元測試
pytest-asyncio>=0.23        # async 測試(future-proof)

# --- 注意 ---
# tomllib 是 Python 3.11+ stdlib,不需要安裝
# sqlite3 是 stdlib,不需要安裝
